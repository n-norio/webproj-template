define([
    'jquery',
    'underscore',
    'backbone',
], function($, _, Backbone) {
    var SampleListView = Backbone.View.extend({
        initialize: function(){
            console.log("###")
        }
    });
})
